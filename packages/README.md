# Package Staging

This directory is reserved for package staging when a current A+ capability is
selected for release.

Do not keep stale package directories here as examples. A package directory
should match an actively retained plugin in `plugins/`, include fresh metadata,
and be removed when that packaged capability is no longer the current release
artifact.
